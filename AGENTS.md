# Codex 项目级工作规则

## 适用范围

- 本文件适用于当前工作区 `D:\PythonProject\sam_kb_0624` 及其所有子目录。
- 默认只允许读取和修改当前工作区内的文件。除非用户明确要求，否则不得访问、修改或删除其他项目目录中的任何内容。
- 可以正常读取和分析当前工作区内的整个项目，以理解代码结构、调用关系和上下文。

## 受保护的环境与文件

- `.venv` 是当前项目已经配置好的 uv 虚拟环境。除非用户明确要求，否则不得删除、重建或修改 `.venv`。
- 除非用户明确要求，否则不得修改 `.env` 中的任何内容。
- 除非用户明确要求，否则不得修改 `pyproject.toml` 或 `uv.lock`。
- 除非用户明确要求，否则不得安装、升级、降级或删除任何 Python 依赖。
- 不得擅自删除源代码、项目资料、数据文件或配置文件。

## 修改原则

- 修改代码时优先采用最小改动原则，只处理当前需求直接涉及的内容。
- 不得因为局部问题擅自进行大规模重构、无关清理或批量格式化。
- 如果用户需求存在歧义，并且不同理解可能导致明显不同的代码修改，必须先向用户确认，再实施修改。

## 高风险操作

- 涉及删除文件、修改依赖、修改环境，或执行其他具有潜在破坏性的命令前，必须先向用户说明准备执行的操作、影响范围和目标。
- 执行此类操作时必须严格限制目标范围，不得扩展到当前工作区之外。

## 修改后的说明

- 修改代码后，必须说明修改了哪些文件、具体修改内容以及修改原因。
